# دليل تركيب الغلاف على GitHub — نظام مصنع الأمل

## الخطوة 1: هات رابط تطبيقك من Apps Script

1. افتح مشروع Apps Script بتاعك
2. Deploy → Manage deployments
3. انسخ الرابط اللي شكله شبه:
   `https://script.google.com/macros/s/AKfycb.../exec`

## الخطوة 2: عدّل ملف index.html

افتح ملف `index.html` اللي جوه المجلد ده، ودور على النص ده:
```
PASTE_YOUR_APPS_SCRIPT_URL_HERE
```
واستبدله بالرابط اللي نسخته في الخطوة 1.

## الخطوة 3: ارفع الملفات على GitHub

جوه نفس المستودع (Repository) بتاع موقعك الشغال على GitHub Pages:
1. اعمل مجلد جديد بأي اسم تحبه (مثلاً `alamal-app`)
2. ارفع الملفات دي كلها جواه:
   - `index.html`
   - `manifest.json`
   - `service-worker.js`
   - `icon-192.png`
   - `icon-512.png`
   - `icon-192-maskable.png`
   - `icon-512-maskable.png`
   - `apple-touch-icon.png`
3. اعمل Commit للتغييرات

## الخطوة 4: افتح الرابط الجديد

الرابط هيكون شكله:
`https://اسم_حسابك.github.io/اسم_المستودع/alamal-app/`

افتحه من على الموبايل، ولازم تلاقي شريط أو زرار "تثبيت" / "Install app" / "Add to Home Screen" بيظهر تلقائي في المتصفح (كروم بيعمل كده لوحده لو الشروط مستوفاة).

## ملحوظات مهمة

- **البيانات لسه بتروح لنفس Google Sheet بالظبط** — الغلاف ده بس بيفتح تطبيقك القديم جوه إطار كامل الشاشة، مفيش أي تغيير في الاتصال بالبيانات.
- **لازم نت شغال** عشان التطبيق يشتغل (لأنه بيتصل بجوجل شيت لايف) — اللي بيتخزن مؤقتًا هو بس شكل الغلاف (الأيقونة وشاشة التحميل) عشان يفتح بسرعة.
- لو غيّرت حاجة تاني في كود Apps Script بعد كده، مش محتاج تعمل حاجة هنا — الغلاف بيفتح آخر نسخة منشورة أوتوماتيك.
